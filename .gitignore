node_modules/
.hugo_build.lock
package-lock.json

# Build artefacts (regenerated by `npm run build`) — kept in-tree on purpose
# so that end users who only consume the theme do NOT need Node/Tailwind.
# Leave them tracked; uncomment here only if you regenerate on every build.
assets/css/theme.css
static/lib/
public/
