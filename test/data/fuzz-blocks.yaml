# Block pool for the auto-generated fuzz cases (test/content/fuzz/_content.gotmpl).
#
# Each case's body is assembled by concatenating several of these blocks in a
# rotating order, so the pages themselves are never hard-coded. Keep every entry
# a self-contained Markdown fragment and avoid Hugo shortcode syntax.

blocks:
  - |
    ## Generated heading

    A paragraph with **bold**, *italic*, ***bold italic***, `inline code`,
    ~~strikethrough~~ and a [link](https://example.org).
  - |
    | Column A | Column B | Column C |
    |:---------|:--------:|---------:|
    | one      | two      | three    |
    | 4        | 5        | 6        |
    | a very long cell that should wrap inside the card | short | short |
  - |
    - first item
    - second item
      - nested item
      - another nested item
    - third item with **bold** text
  - |
    > A generated blockquote.
    >
    > It has a second paragraph to exercise spacing.
  - |
    ```go
    package main

    import "fmt"

    func main() {
    	fmt.Println("generated go block")
    }
    ```
  - |
    ```python {hl_lines=[2, 4]}
    def generated():
        # highlighted line
        value = 21 * 2
        # highlighted line
        return value
    ```
  - |
    $$
    \int_{0}^{1} x^{2} \, dx = \frac{1}{3}
    $$
  - |
    Inline math like $e^{i\pi} + 1 = 0$ mixed into a normal sentence.
  - |
    1. first
    2. second
    3. third
  - |
    - [x] generated task done
    - [ ] generated task pending
  - |
    ---

    A paragraph carrying a footnote.[^generated]

    [^generated]: Generated footnote text.
  - |
    ![Generated image](/avatar.svg)
  - |
    <div class="generated-block">
      <p>Raw <strong>HTML</strong> block with <em>tags</em>.</p>
    </div>
  - |
    - Level 1
      - Level 2
        - Level 3
          - Level 4
            - Level 5
  - |
    ### Generated sub heading

    Text under a generated `h3` so the table of contents gets more than one entry.
  - |
    A line with a very long inline code span
    `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa`
    followed by more text.
  - |
    ## Second generated heading

    Another paragraph, this time with an image-free body and a trailing
    [reference link](https://gohugo.io "Hugo").
